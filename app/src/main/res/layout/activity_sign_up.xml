<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    style="@style/LoginBackgroundStyle"
    tools:context=".SignUpActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_horizontal"
        android:orientation="vertical">

        <!-- Welcoming Header -->
        <TextView
            android:id="@+id/tv_welcome"
            style="@style/SignUpHeaderTextStyle"
            android:text="Join Us!"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

        <!-- App Logo -->
        <ImageView
            android:id="@+id/logo"
            style="@style/SignUpLogoStyle"
            android:contentDescription="App Logo"
            android:src="@drawable/ic_app_logo"
            android:layout_width="150dp"
            android:layout_height="150dp" />

        <!-- Email Input -->
        <EditText
            android:id="@+id/et_email"
            style="@style/SignUpInputStyle"
            android:hint="Email Address"
            android:inputType="textEmailAddress"
            android:layout_width="320dp"
            android:layout_height="48dp" />

        <!-- First Name Input -->
        <EditText
            android:id="@+id/et_first_name"
            style="@style/SignUpInputStyle"
            android:hint="First Name"
            android:inputType="textPersonName"
            android:layout_width="320dp"
            android:layout_height="48dp" />

        <!-- Last Name Input -->
        <EditText
            android:id="@+id/et_last_name"
            style="@style/SignUpInputStyle"
            android:hint="Last Name"
            android:inputType="textPersonName"
            android:layout_width="320dp"
            android:layout_height="48dp" />

        <!-- Password Input -->
        <EditText
            android:id="@+id/et_password"
            style="@style/SignUpInputStyle"
            android:hint="Password"
            android:inputType="textPassword"
            android:layout_width="320dp"
            android:layout_height="48dp" />

        <!-- Confirm Password Input -->
        <EditText
            android:id="@+id/et_confirm_password"
            style="@style/SignUpInputStyle"
            android:hint="Confirm Password"
            android:inputType="textPassword"
            android:layout_width="320dp"
            android:layout_height="48dp" />

        <!-- Checkbox to Show Password -->
        <CheckBox
            android:id="@+id/cb_show_password"
            style="@style/SignUpCheckBoxStyle"
            android:text="Show Password"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

        <!-- Button Layout with Register and Back buttons -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:gravity="center_horizontal"
            android:orientation="horizontal"
            android:weightSum="2">

            <!-- Back Button -->
            <Button
                android:id="@+id/btn_back"
                style="@style/SignUpBackButtonStyle"
                android:text="Back"
                android:layout_width="160dp"
                android:layout_height="50dp" />

            <!-- Register Button -->
            <Button
                android:id="@+id/btn_register"
                style="@style/SignUpRegisterButtonStyle"
                android:text="Register"
                android:layout_width="160dp"
                android:layout_height="50dp"
                android:layout_marginLeft="16dp" />
        </LinearLayout>

    </LinearLayout>
</ScrollView>
